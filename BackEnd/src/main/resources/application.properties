# Spring Application Configuration
spring.application.name=BackEnd

# MongoDB Atlas Configuration - Optimized for Atlas Connection
spring.data.mongodb.database=shohay
spring.data.mongodb.uri=mongodb+srv://shohay:BaGZMypYiu8erP5X@shohaycluster.fowvyur.mongodb.net/shohay?retryWrites=true&w=majority&appName=ShohayCluster&authSource=admin&ssl=true
#spring.data.mongodb.uri=mongodb://localhost:27017/shohay

# MongoDB Atlas Connection Pool Configuration
spring.data.mongodb.connection-pool.max-size=20
spring.data.mongodb.connection-pool.min-size=5
spring.data.mongodb.connection-pool.max-wait-time=5000ms
spring.data.mongodb.connection-pool.max-connection-idle-time=30000ms
spring.data.mongodb.connection-pool.max-connection-life-time=60000ms

# MongoDB Atlas SSL Configuration
spring.data.mongodb.ssl.enabled=true
spring.data.mongodb.ssl.invalid-host-name-allowed=true

# JWT Configuration
jwt.secret=404E635266556A586E3272357538782F413F4428472B4B6250645367566B5970
jwt.expiration=86400000

# Admin Configuration
app.admin.secret-key=SHOHAY_ADMIN_2025

# Server Configuration
server.port=8080

# CORS Configuration - Enhanced for React Frontend
spring.web.cors.allowed-origins=http://localhost:3000,http://localhost:3001,http://127.0.0.1:3000,https://localhost:3000
spring.web.cors.allowed-methods=GET,POST,PUT,DELETE,OPTIONS,PATCH,HEAD
spring.web.cors.allowed-headers=*
spring.web.cors.allow-credentials=true
spring.web.cors.max-age=3600

# Static Resources Configuration for React
spring.web.resources.static-locations=classpath:/static/,classpath:/public/,file:src/main/resources/static/
spring.web.resources.cache.period=3600
spring.mvc.static-path-pattern=/static/**

# React Build Directory Configuration
spring.resources.static-locations=classpath:/static/,classpath:/public/,file:build/

# File Upload Configuration
spring.servlet.multipart.enabled=true
spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=10MB
spring.servlet.multipart.location=${java.io.tmpdir}

# Error Handling Configuration
server.error.whitelabel.enabled=false
server.error.include-message=always
server.error.include-binding-errors=always
server.error.include-stacktrace=on_param
server.error.include-exception=false
spring.mvc.throw-exception-if-no-handler-found=true
spring.web.resources.add-mappings=false

# JSON Configuration
spring.jackson.serialization.fail-on-empty-beans=false
spring.jackson.default-property-inclusion=NON_NULL
spring.jackson.serialization.write-dates-as-timestamps=false
spring.jackson.time-zone=UTC
spring.jackson.date-format=yyyy-MM-dd HH:mm:ss

# Development Profile Configuration
spring.profiles.active=dev

# Logging Configuration - Enhanced for Development
logging.level.com.lab.BackEnd=INFO
logging.level.org.springframework.data.mongodb=DEBUG
logging.level.org.springframework.web=INFO
logging.level.org.springframework.security=INFO
logging.level.com.mongodb=INFO
logging.pattern.console=%d{yyyy-MM-dd HH:mm:ss} - %msg%n
logging.pattern.file=%d{yyyy-MM-dd HH:mm:ss} [%thread] %-5level %logger{36} - %msg%n

# Security Configuration
spring.security.user.name=admin
spring.security.user.password=admin123
spring.security.user.roles=ADMIN

# Session Configuration
server.servlet.session.timeout=30m
server.servlet.session.cookie.http-only=true
server.servlet.session.cookie.secure=false
server.servlet.session.cookie.same-site=lax

# Email Configuration
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=${EMAIL_USERNAME:your-email@gmail.com}
spring.mail.password=${EMAIL_PASSWORD:your-app-password}
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.starttls.required=true
spring.mail.properties.mail.smtp.ssl.trust=smtp.gmail.com

# Application Specific Configuration
app.donor.default-currency=BDT
app.ngo.verification.required=true
app.donation.minimum-amount=1.00
app.donation.maximum-amount=100000.00

# Cache Configuration
spring.cache.type=simple
spring.cache.cache-names=donors,ngos,donations,users,profiles
spring.cache.caffeine.spec=maximumSize=1000,expireAfterAccess=600s

# Actuator Configuration
management.endpoints.web.exposure.include=health,info,metrics,env
management.endpoint.health.show-details=when-authorized
management.endpoint.health.show-components=always
management.info.env.enabled=true
management.health.mongo.enabled=true

# Validation Configuration
spring.validation.enabled=true

# MongoDB Specific Configuration
spring.data.mongodb.auto-index-creation=true
spring.data.mongodb.field-naming-strategy=org.springframework.data.mapping.model.SnakeCaseFieldNamingStrategy

# HTTP Configuration
server.compression.enabled=true
server.compression.mime-types=text/html,text/xml,text/plain,text/css,text/javascript,application/javascript,application/json
server.compression.min-response-size=1024

# Tomcat Configuration
server.tomcat.connection-timeout=20000
server.tomcat.max-connections=8192
server.tomcat.threads.max=200
server.tomcat.threads.min-spare=10

# Spring Boot DevTools Configuration (for development)
spring.devtools.restart.enabled=true
spring.devtools.livereload.enabled=true
spring.devtools.add-properties=true

# Transaction Configuration
spring.data.mongodb.transactions.enabled=true

# MongoDB Atlas Specific Settings
spring.data.mongodb.socket-timeout=30000ms
spring.data.mongodb.connect-timeout=10000ms
spring.data.mongodb.server-selection-timeout=30000ms

# Additional CORS Configuration for specific endpoints
management.endpoints.web.cors.allowed-origins=http://localhost:3000,http://localhost:3001
management.endpoints.web.cors.allowed-methods=GET,POST
management.endpoints.web.cors.allowed-headers=*

# Custom Application Properties
app.frontend.url=http://localhost:3000
app.backend.url=http://localhost:8080
app.upload.dir=${user.home}/shohay-uploads
app.jwt.header=Authorization
app.jwt.prefix=Bearer 

# Database Migration Configuration
spring.data.mongodb.repositories.enabled=true

# Internationalization
spring.messages.basename=messages
spring.messages.encoding=UTF-8
spring.messages.cache-duration=3600

# Performance Optimization
spring.jpa.open-in-view=false
spring.data.web.pageable.default-page-size=20
spring.data.web.pageable.max-page-size=100

# Environment Specific Configuration
app.environment=${ENVIRONMENT:development}
app.debug.enabled=${DEBUG_ENABLED:true}

# Rate Limiting Configuration
app.rate-limit.enabled=true
app.rate-limit.requests-per-minute=100

# Backup Configuration
app.backup.enabled=false
app.backup.schedule=0 0 2 * * ?
